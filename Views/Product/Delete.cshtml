@model ShoppingCartApp.Models.Product

@{
    ViewData["Title"] = "Delete Product";
}

<div class="container mt-4">
    <h2 class="text-center text-danger fw-bold">🗑 Delete Product</h2>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow p-4">
                <h4 class="text-center">Are you sure you want to delete this product?</h4>

                <form asp-action="Delete" method="post">
                    <div class="form-group">
                        <label class="fw-bold">Product Name:</label>
                        <input asp-for="ProductName" class="form-control" readonly />
                    </div>
                    <div class="form-group">
                        <label class="fw-bold">Price:</label>
                        <input asp-for="Price" class="form-control" readonly />
                    </div>

                    <div class="text-center my-3">
                        <label class="fw-bold">Product Image</label>
                        <img src="@Model.ProductImage" alt="Product Image" class="img-fluid rounded shadow-sm" style="max-width: 200px;">
                    </div>

                    <div class="d-flex justify-content-center mt-3">
                        <button type="submit" class="btn btn-danger mx-2">🗑 Delete</button>
                        <a asp-action="Index" class="btn btn-secondary">↩ Back to List</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
